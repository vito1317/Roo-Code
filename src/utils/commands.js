import { Package } from "../shared/package";
export const getCommand = (id) => `${Package.name}.${id}`;
export const getCodeActionCommand = (id) => `${Package.name}.${id}`;
export const getTerminalCommand = (id) => `${Package.name}.${id}`;
//# sourceMappingURL=commands.js.map